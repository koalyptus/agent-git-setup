# legacy local worktrees from pre-~/.agent-git-setup default; safe to keep
.worktrees/

# one-time GitHub App credentials file (holds App ID + PEM path; chmod 600)
# never commit this — it lets the holder mint bot tokens as your app
agent-git-setup-credentials.env
**/agent-git-setup/credentials.env
